# appimage-builder recipe see https://appimage-builder.readthedocs.io for details
version: 1
AppDir:
  path: AppDir
  app_info:
    id: com.ansel.app
    name: Ansel
    icon: ansel
    version: latest
    exec: usr/bin/ansel
    exec_args: $@
  files:
    include:
      - /lib64/ld-linux-x86-64.so.2
      - ~/.local/share/lensfun/updates/version_1
    exclude:
      - usr/share/man
      - usr/share/doc/*/README.*
      - usr/share/doc/*/changelog.*
      - usr/share/doc/*/NEWS.*
      - usr/share/doc/*/TODO.*
  apt:
    arch: [ x86_64 ]
    sources:
      - sourceline: 'deb [arch=amd64] http://azure.archive.ubuntu.com/ubuntu jammy main restricted universe multiverse'
      - sourceline: 'deb [arch=amd64] http://azure.archive.ubuntu.com/ubuntu jammy-updates main restricted universe multiverse'
      - sourceline: 'deb [arch=amd64] http://azure.archive.ubuntu.com/ubuntu jammy-backports main restricted universe multiverse'
    include:
      - libavif
      - libcairo2
      - libcolord
      - libcolord-gtk
      - libcups2
      - libcurl4-gnutls
      - libexiv2
      - libimage-exiftool-perl
      - libgdk-pixbuf2.0
      - libglib2.0-dev
      - libgraphicsmagick1
      - libgtk-3
      - libheif
      - libjpeg
      - libjson
      - liblcms2
      - liblensfun-bin
      - liblensfun-data-v1
      - liblensfun1
      - liblua5.3
      - libgmic
      - libopenexr
      - libopenjp2-7
      - libosmgpsmap-1.0
      - libpango1.0
      - libpng
      - libportmidi
      - libpugixml
      - librsvg2
      - libsdl2
      - libsecret-1
      - libsoup2.4
      - libsqlite3
      - libtiff5
      - libwebp
      - libx11
      - libxml2
      - libxml2-utils
  test:
    fedora-30:
      image: appimagecrafters/tests-env:fedora-30
      command: ./AppRun
    debian-stable:
      image: appimagecrafters/tests-env:debian-stable
      command: ./AppRun
    archlinux-latest:
      image: appimagecrafters/tests-env:archlinux-latest
      command: ./AppRun
    centos-7:
      image: appimagecrafters/tests-env:centos-7
      command: ./AppRun
    ubuntu-xenial:
      image: appimagecrafters/tests-env:ubuntu-xenial
      command: ./AppRun
AppImage:
  arch: x86_64
  update-information: guess
